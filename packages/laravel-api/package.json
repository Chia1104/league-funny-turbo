{
  "name": "@wanin/laravel-api",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "export": {
    ".": "./dist"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint \"{src,apps,libs,test}/**/*.{ts,js,tsx,jsx}\" --fix"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "@wanin/eslint-config-base": "workspace:0.0.0",
    "tsconfig": "workspace:0.0.0",
    "tsup": "^6.5.0"
  }
}
