# Nuxt2 project

This has been deprecated.

## Development

Run the following command to generate the `.env` file:

```bash
cp apps\nuxt2\.env.example apps\nuxt2\.env
```

Then, run the following command to start the development server:

```bash
pnpm dev --filter nuxt2...
```
